% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpe_surrogate.R
\name{sample_tpe}
\alias{sample_tpe}
\title{Sample New Configurations Using TPE Acquisition Function}
\usage{
sample_tpe(surrogate, n_samples, param_bounds, param_types)
}
\arguments{
\item{surrogate}{A fitted TPE surrogate model object returned by
\code{\link{fit_tpe_surrogate}}.}

\item{n_samples}{Integer specifying the number of configurations to return.}

\item{param_bounds}{A named list of parameter bounds (see
\code{\link{generate_uniform_samples}}).}

\item{param_types}{A named list of parameter types (see
\code{\link{generate_uniform_samples}}).}
}
\value{
A data frame containing n_samples configurations selected based
on the highest expected improvement scores.
}
\description{
Samples new configurations from a fitted TPE surrogate model by
maximizing the expected improvement (EI) acquisition function,
defined as the ratio of good to bad density.
}
\details{
The function oversamples candidates (20x the requested number) from the
good distribution using rejection sampling, then selects the top n_samples
based on the acquisition function p(x|good)/p(x|bad).
}
\examples{
\dontrun{
# Assuming surrogate is already fitted
new_configs <- sample_tpe(
  surrogate,
  n_samples = 32,
  param_bounds,
  param_types
)
}
}
